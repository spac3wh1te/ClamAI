# ClamAI Service 代码结构

## 概述

ClamAI Service 是一个基于 Go 的 API 代理网关服务，采用 **Provider-native 透明代理架构**，按模型服务商原生协议透传转发，支持用户认证、安全审核、流量统计等功能。

- **端口**：Proxy 端口（模型 API）默认 8080，Admin 端口（管理 API）默认 8081
- **模式**：`server` 模式（`--host 0.0.0.0`）提供完整 Web UI + API；默认模式为本地代理模式
- **数据库**：SQLite（`clamai.db`），支持用户、API Key、提供商、流量记录等数据持久化

---

## 文件清单

### 入口与核心

| 文件 | 功能 |
|------|------|
| `main.go` | 服务入口，解析命令行参数，启动 proxy 和 admin 两个 HTTP 服务器 |
| `server.go` | ProxyServer 结构体定义，动态路由注册（setupRoutes），中间件挂载 |
| `types.go` | 通用类型定义（Config、TokenDetail、ModelInfo 等），initLogging 日志初始化 |

### Provider 路由与字段提取

| 文件 | 功能 |
|------|------|
| `provider_routes.go` | ProviderRouteSpec 路由规格定义、DefaultProviderRoutes 默认路由表、路径匹配与重写、context 辅助函数 |
| `field_extraction.go` | JSON 字段精准提取工具：extractTextsForSecurity、extractModelFromBody、extractTokensFromBody |

### 认证

| 文件 | 功能 |
|------|------|
| `auth.go` | Auth 处理器入口，JWT 签名/验证核心逻辑 |
| `auth_jwt.go` | JWT 工具函数：GenerateToken、isValidJWT、getAuthClaimsFromRequest |
| `auth_middleware.go` | adminAuthMiddleware（管理 API 鉴权）、authMiddleware（代理 API 鉴权）|
| `auth_handlers.go` | 认证相关 HTTP 处理器：login、setup、register、token refresh、me |
| `auth_users.go` | 用户管理路由：CRUD 用户、密码重置、注册开放控制 |

### 数据库

| 文件 | 功能 |
|------|------|
| `db.go` | 数据库初始化、表创建、数据库锁 |
| `db_init.go` | 数据库初始化逻辑 |
| `db_users.go` | 用户表操作：dbListUsers、dbCreateUser、dbUpdateUser、dbDeleteUser |
| `db_apikeys.go` | API Key 表操作：dbCreateAPIKey、dbListAPIKeys、dbUpdateAPIKey、dbDeleteAPIKey |
| `db_settings.go` | 配置存储操作：dbLoadConfig、dbSaveConfig |
| `db_stats.go` | 流量统计操作：dbRecordUsage、dbGetUsageStats、dbGetRecentLogs |
| `db_tasks.go` | 异步任务表操作：分析任务、技能任务（含 dbGetSkillsTaskByID、dbUpdateAnalysisTask）；统一任务编号（T%04d），启动时从 DB 初始化计数器 |
| `db_adapter.go` | 数据库适配层 |

### 提供商

| 文件 | 功能 |
|------|------|
| `providers.go` | Provider 接口定义、NewProvider 工厂函数、fetchModelsForProvider 工具函数 |
| `providers_openai.go` | OpenAI、DeepSeek、SiliconFlow、Qwen、Moonshot、Yi、OpenRouter、Doubao 提供商实现 |
| `providers_anthropic.go` | Anthropic、GLMCoding（智谱 Coding）、MiniMaxTokenPlan 提供商实现 |
| `providers_cn.go` | Gemini、MiniMax、GLM、ArkCode 提供商实现 |

### 代理处理器（Proxy Handlers）

| 文件 | 功能 |
|------|------|
| `handlers_proxy.go` | 透明代理核心处理器：handleTransparentProxy（统一转发）、handleListAllModels（全局模型列表）|
| `handlers_proxytest.go` | 代理测试处理器：handleProxyTest（连通性测试）、handleProxyTestChat（聊天测试）|

### 管理 API 处理器

| 文件 | 功能 |
|------|------|
| `handlers_admin.go` | 管理 API 处理器：提供商管理、API Key 管理、配置管理、用户管理、统计 |
| `handlers_analysis.go` | AI 分析任务处理器：SkillsDetection、CallerProfile、AgentLogAudit、AgentEnvCheck。Skills 检测内置提示词注入防护：用 `<document>` 标签包裹待检测内容，system prompt 强调只做分析不执行指令 |
| `handlers_agent.go` | Agent 任务处理器：handleAgentLogScan、handleAgentEnvCheck、handleAgentDiscover、handleAgentDeepCheck、handleAgentPushSkills（推送 Skills 文件至检测任务） |
| `handlers_apikey.go` | API Key 专项处理器：handleListAPIKeys、handleCreateAPIKey、handleRevealAPIKey |

### 中间件

| 文件 | 功能 |
|------|------|
| `middleware.go` | 核心中间件：corsMiddleware（跨域）、providerMatchMiddleware（路由匹配）、requestTrackingMiddleware（用量统计）、authMiddleware（代理鉴权）、rateLimitMiddleware、securityMiddleware |

### 安全

| 文件 | 功能 |
|------|------|
| `security.go` | 安全审核入口，ContentCheck 主函数 |
| `security_check.go` | 关键词审核、图片安全审核实现 |
| `security_config.go` | 安全配置（关键词等级、屏蔽词配置）加载/保存 |
| `security_writer.go` | 安全告警写入数据库 |

### 其他

| 文件 | 功能 |
|------|------|
| `ratelimit.go` | 流量限制路由与配置管理 |
| `vector.go` | 向量样本管理（安全样本库）路由与 CRUD |
| `tls.go` | TLS 证书生成与加载 |
| `proxy_client.go` | 代理客户端（内部调用下游模型）|
| `internal_call.go` | 内部回调处理（OAuth 等）|
| `frontend_server.go` | 前端静态文件服务（SPA fallback）|
| `frontend_stub.go` | 非 server 模式下的前端桩（空实现）|
| `config_api.go` | /api/v1/app/info 等配置类 API；配置保存时调用 SetLogLevel 动态生效 |
| `logger.go` | 日志系统：initLogger（结构化 JSON 日志）、levelFilterWriter（按 `[DEBUG/INFO/WARN/ERROR]` 前缀过滤 log.Printf）、SetLogLevel（运行时动态调整日志级别） |

### 测试

| 文件 | 功能 |
|------|------|
| `types_test.go` | 类型相关单元测试 |
| `security_check_test.go` | 安全审核单元测试 |

---

## 服务端口与路由总览

```
Proxy Server (默认 :8080) — Provider-native 透明代理
├── /openai/v1/*             → https://api.openai.com/v1/*          (bearer 认证)
├── /anthropic/v1/*          → https://api.anthropic.com/v1/*       (x-api-key 认证)
├── /siliconflow/v1/*        → https://api.siliconflow.cn/v1/*      (bearer 认证)
├── /deepseek/v1/*           → https://api.deepseek.com/v1/*        (bearer 认证)
├── /minimax/v1/*            → https://api.minimax.chat/v1/*        (bearer 认证)
├── /minimax-tokenplan/v1/*  → https://api.minimaxi.com/anthropic/v1/* (x-api-key 认证)
├── /glm/v1/*                → https://open.bigmodel.cn/api/paas/v4/*  (bearer 认证)
├── /glm-coding/v1/*         → https://open.bigmodel.cn/api/coding/paas/v4/* (bearer 认证)
├── /doubao/v1/*             → https://ark.cn-beijing.volces.com/api/v3/* (bearer 认证)
├── /arkcode/v1/*            → https://ark.cn-beijing.volces.com/api/coding/v3/* (bearer 认证)
├── /qwen/v1/*               → https://dashscope.aliyuncs.com/compatible-mode/v1/* (bearer 认证)
├── /moonshot/v1/*           → https://api.moonshot.cn/v1/*         (bearer 认证)
├── /yi/v1/*                 → https://api.lingyiwanwu.com/v1/*     (bearer 认证)
├── /openrouter/v1/*         → https://openrouter.ai/api/v1/*       (bearer 认证)
└── /v1/models               → 全局模型列表（汇总所有 provider）

Admin Server (默认 :8081)
├── /health               GET    健康检查
├── /oauth/callback       GET    OAuth 回调
├── /api/v1/auth/*        -      认证相关（status/login/setup/register/refresh/me）
├── /api/v1/users/*       -      用户管理
├── /api/v1/providers/*   -      提供商管理
├── /api/v1/keys/*        -      API Key 管理
├── /api/v1/config        GET/PUT 配置读写
├── /api/v1/profiles/*     -      配置 Profile 管理
├── /api/v1/stats/*        -      流量统计
├── /api/v1/security/*     -      安全配置与告警
├── /api/v1/ratelimit/*    -      限流配置
├── /api/v1/analysis/*     -      AI 分析任务
├── /api/v1/skills/*       -      技能检测任务（含 stop）
├── /api/v1/agent/*       -      Agent 工具（含 push-skills）
└── /admin/*              -      静态文件（SPA）
```

## 中间件执行顺序

```
corsMiddleware → providerMatchMiddleware → apiLoggingMiddleware → rateLimitMiddleware → securityMiddleware → requestTrackingMiddleware → authMiddleware
```

## ProviderRouteSpec 配置结构

每个 provider 的路由映射和字段提取规则通过 `ProviderRouteSpec` 集中定义：

```go
type ProviderRouteSpec struct {
    Name            string             // "minimax", "siliconflow"
    PathPrefix      string             // "/minimax/v1" — 对外暴露的路径前缀
    UpstreamBase    string             // "https://api.minimax.chat" — 上游地址
    UpstreamRewrite string             // "/v1" — 路径重写规则
    AuthType        string             // "bearer" 或 "x-api-key"
    Security        SecurityExtraction // 安全审核字段提取规则
    Usage           UsageExtraction    // 用量统计字段提取规则
}
```

路径重写示例：
- 请求 `/minimax/v1/chat/completions` → 去掉前缀 `/chat/completions` → 拼接 rewrite `/v1/chat/completions` → 上游 `https://api.minimax.chat/v1/chat/completions`
- 请求 `/glm/v1/chat/completions` → 去掉前缀 `/chat/completions` → rewrite 为空 → 上游 `https://open.bigmodel.cn/api/paas/v4/chat/completions`

## 构建

- `go build -tags server` 嵌入前端静态文件（server 模式）
- 默认构建不包含前端，用于 Tauri 桌面客户端集成
